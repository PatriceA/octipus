import type { RoleMeta } from '../types';
export const meta: RoleMeta = {
  role: 'qa',
  toolIds: ['browser', 'browser-ext', 'shell', 'docker', 'filesystem', 'knowledge', 'task_state', 'visual'],
  defaultTopic: 'qa',
};

import type { RoleMeta } from '../types';
export const meta: RoleMeta = {
  role: 'coding',
  toolIds: ['filesystem', 'shell', 'git', 'knowledge', 'task_state', 'repo_registry', 'mcp'],
  defaultTopic: 'coding',
};

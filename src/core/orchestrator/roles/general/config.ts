import type { RoleMeta } from '../types';
export const meta: RoleMeta = {
  role: 'general',
  toolIds: ['filesystem', 'browser-ext', 'websearch', 'messaging', 'knowledge', 'scheduling', 'profiles', 'email-processor', 'artifacts', 'mcp'],
  defaultTopic: 'general',
};

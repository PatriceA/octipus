import type { RoleMeta } from '../types';
export const meta: RoleMeta = {
  role: 'research',
  toolIds: ['browser', 'browser-ext', 'websearch', 'knowledge', 'task_state', 'filesystem', 'profiles', 'artifacts', 'artifacts_toolbox', 'mcp'],
  defaultTopic: 'research',
};

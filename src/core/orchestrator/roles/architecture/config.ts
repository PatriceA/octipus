import type { RoleMeta } from '../types';
export const meta: RoleMeta = {
  role: 'architecture',
  toolIds: ['filesystem', 'shell', 'knowledge', 'task_state', 'websearch', 'mcp'],
  defaultTopic: 'architecture',
};

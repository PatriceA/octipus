{
  "name": "weather",
  "version": "1.0.0",
  "description": "Fetches current weather and multi-day forecasts from wttr.in",
  "author": "patrice",
  "main": "index.ts",
  "tools": [
    {
      "name": "get_weather",
      "description": "Get the current weather for a city, including temperature, condition, humidity, and wind speed",
      "parameters": {
        "city": {
          "type": "string",
          "description": "The city name to get weather for (e.g. 'London', 'New York')",
          "required": true
        }
      }
    },
    {
      "name": "get_forecast",
      "description": "Get a multi-day weather forecast for a city",
      "parameters": {
        "city": {
          "type": "string",
          "description": "The city name to get the forecast for",
          "required": true
        },
        "days": {
          "type": "number",
          "description": "Number of forecast days (1-3)",
          "required": false,
          "default": 3
        }
      }
    }
  ]
}
